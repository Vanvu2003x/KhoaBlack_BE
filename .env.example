# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=khoablack

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key
JWT_EXPIRY=7d

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS Configuration
# Danh sách các origin được phép (phân cách bằng dấu phẩy)
CORS_ORIGINS=http://localhost:3000

# Socket.IO CORS Configuration
# Trong development, để trống để cho phép tất cả
# Trong production, set các URL frontend:
SOCKET_ORIGINS=http://localhost:3000

# Frontend URL (fallback cho Socket CORS trong production)
FRONTEND_URL=http://localhost:3000

# Email Configuration (nếu có)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Payment Gateway (nếu có)
# WEB2M_TOKEN=your-web2m-token

# Morishop API Configuration
MORISHOP_API_KEY=your-morishop-api-key

# NapGame247 Configuration
NAPGAME247_API_KEY=your-napgame247-api-key
